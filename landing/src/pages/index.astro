---
import Layout from '../layouts/Layout.astro';
import { siWhatsapp, siTelegram, siDiscord } from 'simple-icons';

// Slack icon path (simple-icons doesn't have siSlack)
const slackIcon = "M5.042 15.165a2.528 2.528 0 0 1-2.52 2.523A2.528 2.528 0 0 1 0 15.165a2.527 2.527 0 0 1 2.522-2.52h2.52v2.52zM6.313 15.165a2.527 2.527 0 0 1 2.521-2.52 2.527 2.527 0 0 1 2.521 2.52v6.313A2.528 2.528 0 0 1 8.834 24a2.528 2.528 0 0 1-2.521-2.522v-6.313zM8.834 5.042a2.528 2.528 0 0 1-2.521-2.52A2.528 2.528 0 0 1 8.834 0a2.528 2.528 0 0 1 2.521 2.522v2.52H8.834zM8.834 6.313a2.528 2.528 0 0 1 2.521 2.521 2.528 2.528 0 0 1-2.521 2.521H2.522A2.528 2.528 0 0 1 0 8.834a2.528 2.528 0 0 1 2.522-2.521h6.312zM18.956 8.834a2.528 2.528 0 0 1 2.522-2.521A2.528 2.528 0 0 1 24 8.834a2.528 2.528 0 0 1-2.522 2.521h-2.522V8.834zM17.688 8.834a2.528 2.528 0 0 1-2.523 2.521 2.527 2.527 0 0 1-2.52-2.521V2.522A2.527 2.527 0 0 1 15.165 0a2.528 2.528 0 0 1 2.523 2.522v6.312zM15.165 18.956a2.528 2.528 0 0 1 2.523 2.522A2.528 2.528 0 0 1 15.165 24a2.527 2.527 0 0 1-2.52-2.522v-2.522h2.52zM15.165 17.688a2.527 2.527 0 0 1-2.52-2.523 2.526 2.526 0 0 1 2.52-2.52h6.313A2.527 2.527 0 0 1 24 15.165a2.528 2.528 0 0 1-2.522 2.523h-6.313z";

const siIcon = (icon: any) => icon.path;

const platforms = [
  { name: 'WhatsApp', icon: siIcon(siWhatsapp), color: '#25D366', desc: 'Link your WhatsApp in seconds', action: 'Scan QR Code' },
  { name: 'Telegram', icon: siIcon(siTelegram), color: '#26A5E4', desc: 'Create bot & paste token', action: 'Bot Token' },
  { name: 'Discord', icon: siIcon(siDiscord), color: '#5865F2', desc: 'Authorize with Discord', action: 'One-Click OAuth' },
  { name: 'Slack', icon: slackIcon, color: '#E01E5A', desc: 'Add to your workspace', action: 'One-Click OAuth' },
];

const features = [
  { title: 'Zaki, Pre-Configured', desc: 'Node.js 22, gateway, workspace, browser automation—all set up and ready. Connect your platforms and go.', icon: '⚡' },
  { title: 'Your API Keys, Your Instance', desc: 'Bring your Anthropic or OpenAI key. Your Zaki runs fully isolated—we never see your conversations.', icon: '🔐' },
  { title: 'One-Click Connections', desc: 'Connect WhatsApp, Telegram, Discord, or Slack from your dashboard. No terminal commands, no config files.', icon: '🔗' },
  { title: 'Simple Dashboard', desc: 'Manage your instance, connect platforms, and update settings through an easy-to-use web dashboard.', icon: '📊' },
  { title: 'Web & Computer Use', desc: 'Browser automation baked in. Your Zaki can browse sites, fill forms, and fetch live data out of the box.', icon: '🌐' },
  { title: 'Your Own VPS', desc: 'Your own isolated instance—not a shared container. Consistent performance with full process isolation.', icon: '🖥️' },
];

const faqs = [
  { q: 'Is Zaki the same as OpenClaw/Clawdbot?', a: 'Zaki is built on OpenClaw technology but fully managed—we handle all the infrastructure so you can focus on using your AI assistant.' },
  { q: 'What\'s the easiest way to run Zaki?', a: 'Sign up, paste your API key, connect your platforms, and start chatting. Under 60 seconds, no technical knowledge required.' },
  { q: 'Do I need my own API keys?', a: 'Free uses Kimi (no key needed). BYOK ($19) = bring your own keys. Premium ($60) = we provide Claude, no keys needed.' },
  { q: 'What platforms can I connect?', a: 'WhatsApp, Telegram, Discord, and Slack. Connect as many as you want from your dashboard.' },
  { q: 'Can I self-host instead?', a: 'Absolutely! Check out OpenClaw docs at docs.openclaw.ai for self-hosting guides.' },
  { q: 'Is my data private?', a: 'Yes. Your instance is fully isolated. We never see your conversations or API keys.' },
  { q: 'What happens if I cancel?', a: 'Cancel anytime. We offer a 7-day money-back guarantee, no questions asked.' },
  { q: 'Does Zaki work with Claude, GPT, or other models?', a: 'Yes! Bring any OpenAI-compatible API key. Claude, GPT-4, and more all work.' },
];
---

<Layout title="Zaki — Run OpenClaw. Skip the Setup.">
  <style>
    :root {
      --bg-deep: #0a0a0f;
      --bg-card: #12121a;
      --bg-card-hover: #1a1a25;
      --coral-bright: #ff6b6b;
      --coral-dark: #cc5555;
      --cyan-bright: #4ecdc4;
      --text-primary: #ffffff;
      --text-secondary: #a0a0b0;
      --border-color: #2a2a3a;
    }
    
    * { box-sizing: border-box; margin: 0; padding: 0; }
    
    body {
      background: var(--bg-deep);
      color: var(--text-primary);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.6;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 24px;
    }
    
    /* Hero */
    .hero {
      text-align: center;
      padding: 120px 0 80px;
    }
    
    .hero-badge {
      display: inline-block;
      background: rgba(255, 107, 107, 0.1);
      border: 1px solid var(--coral-bright);
      color: var(--coral-bright);
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 14px;
      margin-bottom: 24px;
    }
    
    .hero h1 {
      font-size: clamp(40px, 8vw, 72px);
      font-weight: 700;
      line-height: 1.1;
      margin-bottom: 24px;
    }
    
    .hero h1 span {
      background: linear-gradient(135deg, var(--coral-bright), var(--cyan-bright));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    
    .hero-subtitle {
      font-size: 20px;
      color: var(--text-secondary);
      max-width: 600px;
      margin: 0 auto 40px;
    }
    
    .hero-buttons {
      display: flex;
      gap: 16px;
      justify-content: center;
      flex-wrap: wrap;
    }
    
    .btn {
      padding: 14px 28px;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.2s;
      cursor: pointer;
      border: none;
    }
    
    .btn-primary {
      background: var(--coral-bright);
      color: white;
    }
    
    .btn-primary:hover {
      background: var(--coral-dark);
      transform: translateY(-2px);
    }
    
    .btn-secondary {
      background: transparent;
      color: var(--text-primary);
      border: 1px solid var(--border-color);
    }
    
    .btn-secondary:hover {
      border-color: var(--coral-bright);
    }
    
    .hero-stats {
      display: flex;
      gap: 40px;
      justify-content: center;
      margin-top: 60px;
      flex-wrap: wrap;
    }
    
    .hero-stat {
      text-align: center;
    }
    
    .hero-stat-value {
      font-size: 14px;
      color: var(--text-secondary);
    }
    
    .platform-pills {
      display: flex;
      gap: 12px;
      justify-content: center;
      margin-top: 40px;
      flex-wrap: wrap;
    }
    
    .platform-pill {
      display: flex;
      align-items: center;
      gap: 8px;
      background: var(--bg-card);
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 14px;
    }
    
    .platform-pill svg {
      width: 16px;
      height: 16px;
    }
    
    /* Sections */
    section {
      padding: 100px 0;
    }
    
    section + section {
      border-top: 1px solid var(--border-color);
    }
    
    .section-badge {
      color: var(--coral-bright);
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 2px;
      margin-bottom: 16px;
    }
    
    .section-title {
      font-size: clamp(32px, 5vw, 48px);
      font-weight: 700;
      margin-bottom: 16px;
    }
    
    .section-title span {
      color: var(--coral-bright);
    }
    
    .section-subtitle {
      color: var(--text-secondary);
      font-size: 18px;
      max-width: 600px;
      margin: 0 auto;
    }
    
    .section-header {
      margin-bottom: 64px;
    }
    
    .text-center { text-align: center; }
    
    /* Steps */
    .steps {
      display: grid;
      gap: 40px;
      margin-top: 60px;
    }
    
    .step {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 60px;
      align-items: center;
    }
    
    .step:nth-child(even) {
      direction: rtl;
    }
    
    .step:nth-child(even) > * {
      direction: ltr;
    }
    
    .step-number {
      color: var(--coral-bright);
      font-size: 14px;
      font-weight: 600;
      margin-bottom: 8px;
    }
    
    .step-title {
      font-size: 28px;
      font-weight: 600;
      margin-bottom: 16px;
    }
    
    .step-desc {
      color: var(--text-secondary);
      font-size: 16px;
    }
    
    .step-visual {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 24px;
      min-height: 200px;
    }
    
    .step-visual-header {
      display: flex;
      gap: 8px;
      margin-bottom: 16px;
    }
    
    .step-visual-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: var(--border-color);
    }
    
    .step-visual-dot:first-child { background: #ff5f56; }
    .step-visual-dot:nth-child(2) { background: #ffbd2e; }
    .step-visual-dot:nth-child(3) { background: #27ca40; }
    
    .api-input {
      background: var(--bg-deep);
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 12px 16px;
      color: var(--text-secondary);
      font-family: monospace;
      width: 100%;
      margin-top: 16px;
    }
    
    .platform-buttons {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
      margin-top: 16px;
    }
    
    .platform-btn {
      display: flex;
      align-items: center;
      gap: 8px;
      background: var(--bg-deep);
      border: 1px solid var(--border-color);
      border-radius: 8px;
      padding: 12px;
      color: var(--text-primary);
      font-size: 14px;
    }
    
    .platform-btn svg {
      width: 20px;
      height: 20px;
    }
    
    /* Platform Cards */
    .platform-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 24px;
      margin-top: 60px;
    }
    
    .platform-card {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 24px;
      text-align: center;
      transition: all 0.2s;
    }
    
    .platform-card:hover {
      border-color: var(--coral-bright);
      transform: translateY(-4px);
    }
    
    .platform-card-icon {
      width: 48px;
      height: 48px;
      margin: 0 auto 16px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 12px;
    }
    
    .platform-card-icon svg {
      width: 28px;
      height: 28px;
    }
    
    .platform-card h3 {
      font-size: 18px;
      margin-bottom: 8px;
    }
    
    .platform-card p {
      color: var(--text-secondary);
      font-size: 14px;
      margin-bottom: 16px;
    }
    
    .platform-card-action {
      display: inline-block;
      background: var(--bg-deep);
      border: 1px solid var(--border-color);
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 13px;
      color: var(--text-secondary);
    }
    
    /* Features */
    .features-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 24px;
      margin-top: 60px;
    }
    
    .feature-card {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 24px;
    }
    
    .feature-icon {
      font-size: 32px;
      margin-bottom: 16px;
    }
    
    .feature-card h3 {
      font-size: 18px;
      margin-bottom: 8px;
    }
    
    .feature-card p {
      color: var(--text-secondary);
      font-size: 14px;
    }
    
    /* Stats */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 24px;
      text-align: center;
      padding: 40px 0;
    }
    
    .stat-value {
      font-size: 48px;
      font-weight: 700;
      color: var(--coral-bright);
    }
    
    .stat-label {
      color: var(--text-secondary);
      font-size: 14px;
    }
    
    /* Pricing */
    .pricing-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
      max-width: 1000px;
      margin: 0 auto;
    }
    
    @media (max-width: 900px) {
      .pricing-grid {
        grid-template-columns: 1fr;
        max-width: 400px;
      }
    }
    
    .pricing-card {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 16px;
      padding: 40px;
      text-align: center;
      position: relative;
    }
    
    .pricing-card.featured {
      border-color: var(--coral-bright);
    }
    
    .pricing-badge {
      position: absolute;
      top: -12px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--coral-bright);
      color: white;
      padding: 4px 16px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
    }
    
    .pricing-tier {
      font-size: 14px;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 8px;
    }
    
    .pricing-amount {
      font-size: 56px;
      font-weight: 700;
    }
    
    .pricing-period {
      color: var(--text-secondary);
      margin-bottom: 8px;
    }
    
    .pricing-model {
      color: var(--cyan-bright);
      font-size: 14px;
      margin-bottom: 24px;
    }
    
    .pricing-features {
      text-align: left;
      margin: 24px 0;
    }
    
    .pricing-feature {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 8px 0;
      color: var(--text-secondary);
    }
    
    .pricing-feature::before {
      content: '✓';
      color: var(--cyan-bright);
      font-weight: bold;
    }
    
    .pricing-note {
      font-size: 14px;
      color: var(--text-secondary);
      margin-top: 24px;
    }
    
    .btn-free {
      background: var(--bg-deep);
      color: var(--text-primary);
      border: 1px solid var(--border-color);
    }
    
    .btn-free:hover {
      border-color: var(--cyan-bright);
    }
    
    /* FAQ */
    .faq-list {
      max-width: 800px;
      margin: 60px auto 0;
    }
    
    .faq-item {
      border-bottom: 1px solid var(--border-color);
      padding: 24px 0;
    }
    
    .faq-question {
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .faq-question::after {
      content: '+';
      font-size: 24px;
      color: var(--coral-bright);
    }
    
    .faq-answer {
      color: var(--text-secondary);
      margin-top: 16px;
      display: none;
    }
    
    .faq-item.open .faq-answer {
      display: block;
    }
    
    .faq-item.open .faq-question::after {
      content: '−';
    }
    
    /* CTA */
    .cta {
      text-align: center;
      padding: 100px 0;
      border-top: none !important;
    }
    
    .cta-box {
      background: linear-gradient(135deg, rgba(255, 107, 107, 0.1), rgba(78, 205, 196, 0.1));
      border: 1px solid var(--border-color);
      border-radius: 24px;
      padding: 60px;
    }
    
    /* Footer */
    footer {
      border-top: 1px solid var(--border-color);
      padding: 40px 0;
      text-align: center;
      color: var(--text-secondary);
      font-size: 14px;
    }
    
    @media (max-width: 768px) {
      .step {
        grid-template-columns: 1fr;
      }
      .step:nth-child(even) {
        direction: ltr;
      }
      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }
  </style>

  <main>
    <!-- Hero -->
    <section class="hero">
      <div class="container">
        <div class="hero-badge">☁️ The easiest way to deploy OpenClaw</div>
        <h1>Run <span>Zaki</span>.<br/>Skip the Setup.</h1>
        <p class="hero-subtitle">
          Managed hosting for your personal AI assistant. No VPS. No Docker. No SSH.<br/>
          Just your AI, ready to go.
        </p>
        <div class="hero-buttons">
          <a href="#pricing" class="btn btn-primary">Deploy Zaki →</a>
          <a href="#how" class="btn btn-secondary">See How It Works</a>
        </div>
        <div class="hero-stats">
          <div class="hero-stat"><span class="hero-stat-value">✓ Live in 60 seconds</span></div>
          <div class="hero-stat"><span class="hero-stat-value">✓ Your keys, your data</span></div>
        </div>
        <div class="platform-pills">
          <div class="platform-pill">
            <svg viewBox="0 0 24 24" fill="#25D366"><path d={siIcon(siWhatsapp)}/></svg>
            WhatsApp
          </div>
          <div class="platform-pill">
            <svg viewBox="0 0 24 24" fill="#26A5E4"><path d={siIcon(siTelegram)}/></svg>
            Telegram
          </div>
          <div class="platform-pill">
            <svg viewBox="0 0 24 24" fill="#5865F2"><path d={siIcon(siDiscord)}/></svg>
            Discord
          </div>
          <div class="platform-pill">
            <svg viewBox="0 0 24 24" fill="#E01E5A"><path d={slackIcon}/></svg>
            Slack
          </div>
        </div>
      </div>
    </section>

    <!-- How to Deploy -->
    <section id="how" class="container">
      <div class="text-center section-header">
        <div class="section-badge">3 simple steps</div>
        <h2 class="section-title">How to Deploy <span>Zaki</span></h2>
        <p class="section-subtitle">No DevOps. No terminal. No headaches.</p>
      </div>
      
      <div class="steps">
        <div class="step">
          <div>
            <div class="step-number">01</div>
            <h3 class="step-title">Add Your API Key</h3>
            <p class="step-desc">Sign up and paste your Anthropic or OpenAI key. That's the only configuration you'll ever need.</p>
          </div>
          <div class="step-visual">
            <div class="step-visual-header">
              <div class="step-visual-dot"></div>
              <div class="step-visual-dot"></div>
              <div class="step-visual-dot"></div>
            </div>
            <div style="color: var(--text-secondary); font-size: 14px;">API Configuration</div>
            <div style="margin-top: 16px; display: flex; gap: 8px;">
              <span style="background: var(--coral-bright); padding: 6px 12px; border-radius: 6px; font-size: 13px;">Anthropic</span>
              <span style="background: var(--bg-deep); padding: 6px 12px; border-radius: 6px; font-size: 13px; border: 1px solid var(--border-color);">OpenAI</span>
            </div>
            <div class="api-input">sk-ant-api03-••••••••••••</div>
          </div>
        </div>
        
        <div class="step">
          <div>
            <div class="step-number">02</div>
            <h3 class="step-title">Connect Your Platforms</h3>
            <p class="step-desc">Link WhatsApp, Telegram, Discord, or Slack with a single click. No SSH, no config files.</p>
          </div>
          <div class="step-visual">
            <div class="step-visual-header">
              <div class="step-visual-dot"></div>
              <div class="step-visual-dot"></div>
              <div class="step-visual-dot"></div>
            </div>
            <div style="color: var(--text-secondary); font-size: 14px;">Connect Platforms</div>
            <div class="platform-buttons">
              <div class="platform-btn">
                <svg viewBox="0 0 24 24" fill="#25D366"><path d={siIcon(siWhatsapp)}/></svg>
                WhatsApp
              </div>
              <div class="platform-btn">
                <svg viewBox="0 0 24 24" fill="#26A5E4"><path d={siIcon(siTelegram)}/></svg>
                Telegram
              </div>
              <div class="platform-btn">
                <svg viewBox="0 0 24 24" fill="#5865F2"><path d={siIcon(siDiscord)}/></svg>
                Discord
              </div>
              <div class="platform-btn">
                <svg viewBox="0 0 24 24" fill="#E01E5A"><path d={slackIcon}/></svg>
                Slack
              </div>
            </div>
          </div>
        </div>
        
        <div class="step">
          <div>
            <div class="step-number">03</div>
            <h3 class="step-title">Start Chatting</h3>
            <p class="step-desc">Your Zaki is live and ready. Message it from any connected platform—it just works.</p>
          </div>
          <div class="step-visual">
            <div class="step-visual-header">
              <div class="step-visual-dot"></div>
              <div class="step-visual-dot"></div>
              <div class="step-visual-dot"></div>
            </div>
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 20px;">
              <span style="font-size: 24px;">✓</span>
              <span>Total setup time: <strong>Under 60 seconds</strong></span>
            </div>
            <div style="margin-top: 16px; color: var(--text-secondary);">Zero configuration required</div>
          </div>
        </div>
      </div>
    </section>

    <!-- One-Click Integration -->
    <section class="container">
      <div class="text-center section-header">
        <div class="section-badge">One-click integration</div>
        <h2 class="section-title">One-Click Platform <span>Integration</span></h2>
        <p class="section-subtitle">Connect all your messaging platforms from our dashboard. No terminal. No config files.</p>
      </div>
      
      <div class="platform-grid">
        {platforms.map(p => (
          <div class="platform-card">
            <div class="platform-card-icon" style={`background: ${p.color}22;`}>
              <svg viewBox="0 0 24 24" fill={p.color}><path d={p.icon}/></svg>
            </div>
            <h3>{p.name}</h3>
            <p>{p.desc}</p>
            <span class="platform-card-action">{p.action}</span>
          </div>
        ))}
      </div>
    </section>

    <!-- Features -->
    <section class="container">
      <div class="text-center section-header">
        <div class="section-badge">Fully managed infrastructure</div>
        <h2 class="section-title">Everything Zaki Needs.<br/><span>Nothing You Have to Configure.</span></h2>
        <p class="section-subtitle">We handle the infrastructure so you can focus on using Zaki, not maintaining it.</p>
      </div>
      
      <div class="features-grid">
        {features.map(f => (
          <div class="feature-card">
            <div class="feature-icon">{f.icon}</div>
            <h3>{f.title}</h3>
            <p>{f.desc}</p>
          </div>
        ))}
      </div>
    </section>
    
    <!-- Stats -->
    <section class="container">
      <div class="stats-grid">
        <div>
          <div class="stat-value">99.9%</div>
          <div class="stat-label">Uptime SLA</div>
        </div>
        <div>
          <div class="stat-value">&lt;1min</div>
          <div class="stat-label">Deploy Time</div>
        </div>
        <div>
          <div class="stat-value">24/7</div>
          <div class="stat-label">Monitoring</div>
        </div>
        <div>
          <div class="stat-value">0</div>
          <div class="stat-label">Config Required</div>
        </div>
      </div>
    </section>

    <!-- Pricing -->
    <section id="pricing" class="container">
      <div class="text-center section-header">
        <div class="section-badge">Simple pricing</div>
        <h2 class="section-title">Start Free. <span>Scale When Ready.</span></h2>
        <p class="section-subtitle">No credit card required. Upgrade anytime.</p>
      </div>
      
      <div class="pricing-grid">
        <!-- Free Tier -->
        <div class="pricing-card">
          <div class="pricing-tier">Free</div>
          <div class="pricing-amount">$0</div>
          <div class="pricing-period">forever</div>
          <div class="pricing-model">Powered by Kimi K2</div>
          
          <div class="pricing-features">
            <div class="pricing-feature">Your own Zaki instance</div>
            <div class="pricing-feature">Telegram & Discord</div>
            <div class="pricing-feature">No API key needed</div>
            <div class="pricing-feature">Web browsing</div>
            <div class="pricing-feature">Basic support</div>
          </div>
          
          <a href="#" class="btn btn-free" style="width: 100%;">Get Started Free</a>
        </div>
        
        <!-- BYOK Tier -->
        <div class="pricing-card">
          <div class="pricing-tier">Bring Your Keys</div>
          <div class="pricing-amount">$19</div>
          <div class="pricing-period">/month</div>
          <div class="pricing-model">Use your own API keys</div>
          
          <div class="pricing-features">
            <div class="pricing-feature">Everything in Free</div>
            <div class="pricing-feature">All platforms</div>
            <div class="pricing-feature">Any model (Claude, GPT, etc)</div>
            <div class="pricing-feature">Computer use & automation</div>
            <div class="pricing-feature">2GB memory</div>
          </div>
          
          <a href="#" class="btn btn-free" style="width: 100%;">Start with BYOK</a>
        </div>
        
        <!-- Premium Tier -->
        <div class="pricing-card featured">
          <div class="pricing-badge">All Inclusive</div>
          <div class="pricing-tier">Premium</div>
          <div class="pricing-amount">$60</div>
          <div class="pricing-period">/month</div>
          <div class="pricing-model">Claude Sonnet included</div>
          
          <div class="pricing-features">
            <div class="pricing-feature">Everything in BYOK</div>
            <div class="pricing-feature">Claude Sonnet 4 included</div>
            <div class="pricing-feature">No API key needed</div>
            <div class="pricing-feature">Unlimited usage*</div>
            <div class="pricing-feature">Priority support</div>
          </div>
          
          <a href="#" class="btn btn-primary" style="width: 100%;">Go Premium →</a>
          
          <p class="pricing-note">7-day free trial · Cancel anytime</p>
        </div>
      </div>
      
      <p style="text-align: center; color: var(--text-secondary); font-size: 13px; margin-top: 32px;">* Fair use policy applies</p>
    </section>

    <!-- FAQ -->
    <section class="container">
      <div class="text-center section-header">
        <div class="section-badge">FAQ</div>
        <h2 class="section-title">Frequently Asked <span>Questions</span></h2>
        <p class="section-subtitle">Everything you need to know about hosting Zaki.</p>
      </div>
      
      <div class="faq-list">
        {faqs.map(faq => (
          <div class="faq-item">
            <div class="faq-question">{faq.q}</div>
            <div class="faq-answer">{faq.a}</div>
          </div>
        ))}
      </div>
    </section>

    <!-- CTA -->
    <section class="container cta">
      <div class="cta-box">
        <h2 class="section-title">Ready to launch your <span>Zaki</span>?</h2>
        <p class="section-subtitle" style="margin-bottom: 32px;">Deploy your personal AI assistant in under 60 seconds.</p>
        <a href="#pricing" class="btn btn-primary">Start Free Trial →</a>
      </div>
    </section>

    <!-- Footer -->
    <footer>
      <div class="container">
        <p>© 2025 Zaki. Your personal AI assistant.</p>
        <p style="margin-top: 8px;">Built with ❤️ on OpenClaw technology.</p>
      </div>
    </footer>
  </main>

  <script>
    // FAQ toggle
    document.querySelectorAll('.faq-question').forEach(q => {
      q.addEventListener('click', () => {
        q.parentElement.classList.toggle('open');
      });
    });
  </script>
</Layout>
